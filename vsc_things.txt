- req:
	jython installation
	ch/aplu/jGameGrid

- rules:
	u"strings" for non ascii in strings for prints
	# -*- coding: utf-8 -*- is *mandatory* for non ascii in file
	explicit import of builtin java libs/modules

if req.isMet() and rules.isFollowed():
	self.enjoy(vsc)